syntax = "proto3";

package post;

// This go_package makes protoc generate code into your go-project/proto folder
// and importable in Go as "postpb".
option go_package = "/pkg/proto/post;postpb";

import "google/protobuf/timestamp.proto";

service PostService {
  // Get user by ID
  rpc GetPost(GetPostRequest) returns (GetPostResponse);
  rpc GetPostByUserId(GetPostRequest) returns (GetPostResponse);
}

message GetPostRequest {
  string post_id = 1;
}

message GetPostByUserIdRequest {
  string user_id = 1;
}

message GetPostResponse {
  string id = 1;
  string user_id = 2;
  google.protobuf.Timestamp created_at = 3;
  google.protobuf.Timestamp updated_at = 4;

  // Optional fields
  google.protobuf.Timestamp deleted_at = 5;

  string content = 6;
  int64 like_count = 7;
  int64 repost_count = 8;
  int64 comment_count = 9;

  repeated string media = 10;
  repeated string files = 11;
}